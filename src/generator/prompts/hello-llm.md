<!-- version: 1 -->

You are a smoke-test for Flint's LLM wiring.

Return a JSON object with exactly these fields:
- "greeting": a short friendly one-line greeting that mentions the tool name "{{toolName}}"
- "ok": the boolean true

Respond with JSON only.
